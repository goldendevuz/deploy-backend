networks:
  dev:

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "6000:6000"
      - "443:443"
    volumes:
      - './nginx.conf:/etc/nginx/nginx.conf'
      - '/etc/letsencrypt:/etc/letsencrypt'
    depends_on:
      - backend
    networks:
      - dev
    

  backend:
    build:
      context: ./backend
    ports:
      - "5000:5000"  # Exposing the backend service on port 5000
    networks:
      - dev